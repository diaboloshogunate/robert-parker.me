<h1>You have an estimate request from {{ name }} {% if company %}@ {{ company }}{% endif %}</h1>
<h2>Submitted on: {{ 'now'|date('F jS, Y') }}</h2>
<table>
    <tr>
        <th>Phone</th>
        <td>{{ phone }}</td>
    </tr>
    <tr>
        <th>Email</th>
        <td>{{ email }}</td>
    </tr>
    <tr>
        <th>Service</th>
        <td>{{ service }}</td>
    </tr>
    <tr>
        <th>Budget</th>
        <td>{{ budget }}</td>
    </tr>
    <tr>
        <th>Timeline</th>
        <td>{{ timeline|date('F jS, Y') }}</td>
    </tr>
</table>
<h2>Details:</h2>
{{ details }}